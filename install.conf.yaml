- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.zshrc:

- create:
    - ~/dev

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]